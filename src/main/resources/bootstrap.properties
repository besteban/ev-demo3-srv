# this file is loaded by Spring Cloud before application.properties
spring:
  application:
    name: ev-demo3-srv
  cloud:
    config:
      enabled: false
---
spring:
  profiles: dev,uat,prod
  cloud:
    config:
      uri: http://spring-cloud-config.svcs.grupoevo.corp